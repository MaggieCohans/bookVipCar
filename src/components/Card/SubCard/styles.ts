import { StyleSheet } from "react-native";

export default StyleSheet.create({
    title: {
        color: '#f23557',
        fontSize: 18,
        fontWeight: '600',
    },
    subTitle: {
        color: '#000',
        fontSize: 14,
        margin: 8
    }
});